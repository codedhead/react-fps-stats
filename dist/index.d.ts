/// <reference types="react" />
declare type Props = {
    color?: string;
    fontSize?: string;
    capacity: number;
};
export default function FPSStat({ color, fontSize, capacity }: Props): JSX.Element;
export {};
